# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env.production and fill in your actual values
# This template ensures all required environment variables are configured
# for successful email delivery in production deployment (Coolify)

# =============================================================================
# SMTP EMAIL CONFIGURATION (REQUIRED FOR PAYMENT RECEIPTS)
# =============================================================================

# Primary SMTP Configuration (Hostinger)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=contactus@intelliglobalconferences.com
SMTP_PASS=Muni@12345m

# Email Settings
FROM_EMAIL=contactus@intelliglobalconferences.com
FROM_NAME="Intelli Global Conferences"

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================

# Environment (CRITICAL: Must be 'production' for production deployment)
NODE_ENV=production

# Application URL (Update with your actual production domain)
NEXT_PUBLIC_APP_URL=https://your-production-domain.com
NEXTAUTH_URL=https://your-production-domain.com

# =============================================================================
# PAYPAL CONFIGURATION
# =============================================================================

# PayPal Production Configuration
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_production_paypal_client_id
PAYPAL_CLIENT_SECRET=your_production_paypal_client_secret
PAYPAL_BASE_URL=https://api-m.paypal.com

# =============================================================================
# SANITY CMS CONFIGURATION
# =============================================================================

# Sanity Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=your_sanity_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your_sanity_api_token

# =============================================================================
# DATABASE CONFIGURATION (if applicable)
# =============================================================================

# Database URL (Update with your production database)
DATABASE_URL=your_production_database_url

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# NextAuth Secret (Generate a secure random string)
NEXTAUTH_SECRET=your_secure_random_string_here

# =============================================================================
# COOLIFY DEPLOYMENT NOTES
# =============================================================================

# When deploying to Coolify:
# 1. Copy these environment variables to your Coolify application settings
# 2. Replace all placeholder values with your actual production values
# 3. Ensure SMTP_PASS is correctly set (this is the most common failure point)
# 4. Test email delivery using the test-production-email-system.js script
# 5. Monitor application logs for email delivery confirmation

# =============================================================================
# EMAIL DELIVERY TROUBLESHOOTING
# =============================================================================

# If emails are not being delivered in production:
# 
# 1. VERIFY ENVIRONMENT VARIABLES:
#    - Run: node test-production-email-system.js
#    - Check all SMTP_* variables are set correctly
#    - Ensure SMTP_PASS matches your email provider password
#
# 2. CHECK NETWORK CONNECTIVITY:
#    - Ensure production server can reach smtp.hostinger.com:465
#    - Check if firewall blocks SMTP ports (465, 587)
#    - Test with alternative port 587 if 465 fails
#
# 3. VERIFY EMAIL PROVIDER SETTINGS:
#    - Confirm contactus@intelliglobalconferences.com exists
#    - Check if email provider requires app-specific passwords
#    - Verify SMTP is enabled for the email account
#
# 4. MONITOR APPLICATION LOGS:
#    - Look for SMTP connection errors
#    - Check for authentication failures
#    - Monitor email sending success/failure messages
#
# 5. TEST WITH ALTERNATIVE SMTP:
#    - Try port 587 with SMTP_SECURE=false
#    - Test with Gmail SMTP as fallback
#    - Use different email provider if needed

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================

# Before deploying to production:
# ✅ All environment variables are set with production values
# ✅ SMTP credentials are verified and working
# ✅ PayPal is configured for production (not sandbox)
# ✅ Sanity CMS is using production dataset
# ✅ Application URL matches production domain
# ✅ SSL certificates are properly configured
# ✅ Email delivery test has been run successfully
# ✅ Payment flow has been tested end-to-end

# =============================================================================
# MONITORING AND MAINTENANCE
# =============================================================================

# Production monitoring recommendations:
# 1. Set up email delivery monitoring/alerts
# 2. Monitor SMTP connection health
# 3. Track payment receipt delivery success rates
# 4. Log all email sending attempts for debugging
# 5. Set up backup SMTP provider for redundancy

# =============================================================================
# SUPPORT CONTACTS
# =============================================================================

# For email delivery issues:
# - Check application logs first
# - Run test-production-email-system.js for diagnostics
# - Contact email provider support if SMTP issues persist
# - Review Coolify deployment logs for environment variable issues
